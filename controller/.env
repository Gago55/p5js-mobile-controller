# Server URL for local desktop development only.
# On mobile the app auto-detects the server from window.location.hostname,
# so you do NOT need to change this file when testing on a phone over LAN.
VITE_SOCKET_URL=http://localhost:3001
